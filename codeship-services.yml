angular:
  build: client
  dockerfile_path: Dockerfile
  environment:
    - NODE_ENV=test
express:
  build: server
  dockerfile_path: Dockerfile
  environment:
    - NODE_ENV=test
    - MONGO_URL=mongodb://mongo/cash-stash-development
mongo:
  image: mongo:3.4.1
